import { PALLETE } from '../../../../../config/config';
import { styled } from '@mui/system'

export const Text = styled('div')({
    margin: 'normal',
    borderRadius: ' 15px',
    width: '100% !important',
})